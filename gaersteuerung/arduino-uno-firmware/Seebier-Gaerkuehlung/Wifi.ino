void sendTempOverWifi(){
  // temperatureLiquid
  
  if (Wifi.available()){
    WifiData client;
    #define SERVER_ADDR   "seebier.intern.neuamsee.de"
    
    
    //client.connect(SERVER_ADDR, 80);
  }
  
}
